function [output] = KrigMeta(x)
%UNTITLED3 Summary of this function goes here
%   Detailed explanation goes here
load('constants')
load('Krig')


output(1) = predictor(x, d_current);
output(2) = predictor(x, d_force);
disp(['Current = ', num2str()])


end

