load('constants')
time = 0:1/30:1/30*36;
current_t = zeros(1,37);
timestep=1/30;
dwarp=3/1000;
n_weft = round(s_length/dwarp);
L0 = dwarp*pi()/2;
for i = 3:1:37
    %i_tot(t, dwarp, b, gweft, hpoly, hpiezo, L0, e31, kwarp, c, Ypoly, e33)
    current_t(i)= i_tot(i,dwarp,3/1000, .2/1000, 200 *10^-6, 110 * 10^-6, L0,e31, kwarp, c, Ypoly, e33,n_weft);
    %current(i)= i_curv(i, 200 *10^-6, 110 * 10^-6, c, Ypoly, 3/1000, L0, e31,n_weft);
    i_tens(t, dwarp, L0, e31, kwarp, hpiezo, c, hpoly, Ypoly, gweft,n_weft)
    i_ac(t, b, L0, e33, hpiezo, hpoly,n_weft)
    
end
plot(time,current_t)